{
  "name": "cstack-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that serves the claude-token-efficient-setup operating system (agents, docs, operating law, install bundle) as tools.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cstack-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
